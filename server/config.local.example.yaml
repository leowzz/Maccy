server:
  listen_address: ":8080"

database:
  dsn: "postgres://maccy:maccy@127.0.0.1:54329/maccy?sslmode=disable"
  max_connections: 20

account_id: "default"

auth:
  my-macbook: "replace-with-a-long-random-token"

